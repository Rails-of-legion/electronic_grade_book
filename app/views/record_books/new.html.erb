<div class="container mt-5"> 
  <div class="row justify-content-center">
    <div class="col-md-8">
      <h1 class="text-center mb-4"><%= @record_book.new_record? ? 'New Record Book' : 'Edit Record Book' %></h1>
      
      <%= render 'form' %>
    </div>
  </div>
</div>