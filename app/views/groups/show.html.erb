<h1>Group Details</h1>

<p>
  <b>Name:</b> <%= @group.name %>
</p>
<p>
  <b>Curator:</b> <% if @group.curator %> <%= @group.curator.name %> <% else %> Unassigned <% end %>
</p>

<%= link_to "Edit", edit_group_path(@group), class: "btn btn-primary" %>
<%= link_to "Back", groups_path, class: "btn btn-secondary" %>
