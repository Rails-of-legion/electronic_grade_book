<h1>Grade</h1>

<p>Record book: <%= @grade.record_book.subject.name %> (<%= @grade.record_book.student.user.name %>) </p>
<p>Grage: <%= @grade.grade %></p>

<%= link_to 'Edit', edit_grade_path(@grade), class: 'btn btn-default' %>
<%= link_to 'Back', grades_path, class: 'btn btn-secondary' %>
